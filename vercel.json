{
  "buildCommand": "vite build --outDir .site-static --emptyOutDir",
  "outputDirectory": ".site-static",
  "rewrites": [
    { "source": "/((?!api/|assets/).*)", "destination": "/index.html" }
  ]
}
